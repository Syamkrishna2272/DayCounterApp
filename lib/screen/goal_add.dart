
import 'package:flutter/widgets.dart';

class GoalAddingScreen extends StatelessWidget {
  const GoalAddingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}